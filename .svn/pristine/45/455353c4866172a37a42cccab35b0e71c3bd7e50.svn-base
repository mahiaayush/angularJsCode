import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TitleDeliveriesComponent } from './title-deliveries.component';

describe('TitleDeliveriesComponent', () => {
  let component: TitleDeliveriesComponent;
  let fixture: ComponentFixture<TitleDeliveriesComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TitleDeliveriesComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TitleDeliveriesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
